<%= simple_form_for(@contact, remote: true, html: { class: "form-vertical" }) do |f|%>

  <%= f.input :nickname, as: :hidden %>
  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :subject %>
  <%= f.input :message, as: :text %>
  
<% end %>